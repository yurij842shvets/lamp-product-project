export interface ProductVariants {
  id: string;
  label: string;
  image: string;
  description: string;
}

export interface ProductColors {
    id: string;
    label: string;
    className: string
}
export interface ProductParameters {
  label: string;
  value: string;
}
